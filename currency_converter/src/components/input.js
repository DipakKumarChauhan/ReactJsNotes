// This is done when we have multiple components in a single file and we want to export them from a single file.
// THis is a Production approach to avoid multiple import statements in other files.

import InputBox from './InputBox'

export {InputBox}